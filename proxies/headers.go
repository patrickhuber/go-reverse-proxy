package proxies

const (
	// HeaderXForwardedHost is the x-forwarded-host header key
	HeaderXForwardedHost = "X-Forwarded-Host"
	// HeaderXForwardedProto is the x-forwarded-proto header key
	HeaderXForwardedProto = "X-Forwarded-Proto"
	// HeaderXForwardedFor is the x-forwarded-for header key
	HeaderXForwardedFor = "X-Forwarded-For"
	// HeaderXForwardedPath is the x-forwarded-path header key
	HeaderXForwardedPath = "X-Forwarded-Path"
	// HeaderLocation represents the location of a redirect
	HeaderLocation = "Location"
)
